# Secrets — never commit
push/vapid-private.txt
push/subscriptions.json
config.local.js
.env
*.pem

# Node
node_modules/
npm-debug.log*

# Cloudflare wrangler local cache
.wrangler/
proxy/.wrangler/

# OS
.DS_Store

# diagnostic probe artefacts (mount debugging)
.probe-*
*.tmp
.preflight-probe.tmp

# GitHub token for the API fallback publisher
.gh_token
